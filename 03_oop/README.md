# Scoring API

ƒекларативный €зык описани€ и система валидации запросов к HTTP API сервиса скоринга.

### —тру?тура запроса

```
{
    "account": "<им€ компании партнера>", 
    "login": "<им€ пользовател€>", 
    "method": "<им€ метода>", 
    "token": "<аутентификационный токен>", 
    "arguments": {<словарь с аргументами вызываемого метода>}
}
```

* account - стро?а, опционально, может быть пустым
* login - стро?а, об€зательно, может быть пустым
* method - стро?а, об€зательно, может быть пустым
* token - стро?а, об€зательно, может быть пустым 
* arguments - json, об€зательно, может быть пустым

«апрос валиден, если валидны все пол€ по отдельности.

### Cтру?тура ответа
#### OK:
```
{
    "code": <числовой код>, 
    "response": {<ответ вызываемого метода>}
}
```
#### ќшиб?а:
```
{
    "code": <числовой код>, 
    "error": {<сообщение об ошибке>}
}
```
